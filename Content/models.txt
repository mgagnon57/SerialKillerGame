# ==========================================================================
#  HAND-MADE BUILDINGS STANDING ON SURVEYED LOTS - the owner's rulings.
#
#  Each row stands one model from Assets/Noir/Models/<model>.obj on the place
#  whose address matches. The generated town stands down for that lot: no
#  grammar walls, no roof, no frontage - the model brings its own. The place
#  itself stays fully real (door, household, witnesses at the windows); only
#  the LOOK is the owner's.
#
#  address | model | yaw
#
#  yaw is degrees about up, applied after the automatic long-side-to-long-side
#  fit, for models whose front faces the wrong way. Rows are read by
#  CityBuildings; a missing model logs loudly and the lot falls back to the
#  grammars rather than standing empty.
# ==========================================================================

101 Perry St | VictorianHouse | 0
408 Holmes Ave | Residence408 | 0
Rossville unit 7 | PubRow | 0

# A TERRACE ADDRESS CLAIMS THE WHOLE ROW. A row whose address is a terrace's -
# "112 S Chicago", no "#N" - stands ONE model over the union of every unit the
# split minted from it ("112 S Chicago #1", "#2", ...), and every one of those
# units' generated looks stands down. The units stay real places - doors,
# businesses, witnesses - under the one roof the county's own note says they
# always shared.
112 S Chicago | RossvilleStorefront | 0
